# Base API URL used by demo/App.tsx
# Keep trailing /api/ so respond path resolves to /v2/ai-agent/respond/
VITE_API_BASE_URL=http://ncc-stg.api.bawana:8000/

# Required token for Authorization: Bearer <token>
VITE_AI_AGENT_TOKEN=replace-with-access-token

# Optional agent slug (defaults to home-assistant)
VITE_AI_AGENT_AGENT=home-assistant
